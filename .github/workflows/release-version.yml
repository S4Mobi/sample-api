name: Update Project Version

on: create

jobs:
  get-release:
    name: Get the release branch version number
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Project
        uses: actions/checkout@v2

      - name: Install Java and Maven
        uses: actions/setup-java@v1
        with:
          java-version: 11

      - uses: valadas/get-release-branch-version@v1
        id: branchVersion

      - name: Dump the Version
        env:
          MAJOR: ${{ steps.branchVersion.outputs.major }}
          MINOR: ${{ steps.branchVersion.outputs.minor }}
          PATCH: ${{ steps.branchVersion.outputs.patch }}
          NEW_VERSION: ${{ env.MAJOR }}.${{ env.MINOR }}.${{ env.PATCH }}
        run: "echo major: $MAJOR minor: $MINOR patch: $PATCH"

      - name: Update Version in pom.xml
        run: mvn -B versions:set -DnewVersion=${{ env.NEW_VERSION }} -DgenerateBackupPoms=false

      - name: Commit changelog and manifest files
        id: make-commit
        run: |
          git add pom.xml
          git commit --message "Update api version: ${{ env.NEW_VERSION }}"
          echo "::set-output name=commit::$(git rev-parse HEAD)"

      - name: Push new branch
        run: git push origin origin release/${{ env.NEW_VERSION }